project_id = "duldhllwapsjytdzpjfz"

[auth]
site_url = "http://localhost:5173"
additional_redirect_urls = ["http://localhost:5173/admin"]
jwt_expiry = 3600
enable_signup = true

[auth.email]
enable_confirmations = false
enable_signup = true

[auth.external.google]
enabled = true
# These are placeholders for local development. 
# Real values are needed for actual Google sign-in to work.
client_id = "your-client-id"
secret = "your-secret"

[functions.track-click]
verify_jwt = false
